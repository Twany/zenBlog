---
import SectionHeading from '../SectionHeading.astro';
import NoteCard from '../NoteCard.astro';
import { notes } from '../../data/notes';
import { uiLabels } from '../../data/uiLabels';
import type { Locale } from '../../utils/i18n';

interface Props {
  lang: Locale;
}

const { lang } = Astro.props as Props;
---
<div class="max-w-6xl mx-auto px-6">
  <div class="flex items-end justify-between mb-12">
    <div class="flex flex-col gap-4">
      <SectionHeading title={uiLabels.common.fieldNotes[lang]} as="h1" class="mb-0" />
      <p class="font-serif text-slate-600 italic max-w-lg">
        {uiLabels.common.notesDesc[lang]}
      </p>
    </div>
  </div>

  <div class="columns-1 md:columns-2 gap-8 space-y-8 animate-fade-in">
    {notes.map((note) => (
      <NoteCard note={note} lang={lang} class="mb-8" />
    ))}
  </div>
</div>
